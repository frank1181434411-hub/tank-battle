# 开发日志

本文档整合项目开发过程记录，按时间顺序排列。6 月 25 日至 6 月 28 日为接手前已有记录，6 月 29 日为后续地图、碰撞和绘制层级相关开发记录。

## 2026-06-25 工程环境与基础架构

### 阶段目标

完成项目基础目录、编译环境和底层公共类型定义。

### 涉及内容

| 类别 | 内容 |
| -- | -- |
| 项目结构 | 按头文件和源文件分离方式建立 `include/`、`src/` 及核心子模块。 |
| 环境配置 | 配置 C++17 与 SFML 3.1.0 开发环境，解决跨目录包含路径问题。 |
| 公共类型 | 建立 `entity.hpp` 公共实体定义，包含方向、阵营等基础枚举。 |
| 架构约定 | 使用强类型枚举降低模块间耦合，避免早期循环包含问题。 |

### 阶段结果

项目完成基础工程初始化，可以继续添加实体类和核心游戏逻辑。

## 2026-06-26 基础物理与输入控制

### 阶段目标

完成玩家移动、炮弹飞行和射击冷却等基础操作机制。

### 涉及内容

| 类别 | 内容 |
| -- | -- |
| 炮弹系统 | 实现 `Bullet` 类，使用 `deltaTime` 进行帧率无关位移。 |
| 边界处理 | 炮弹飞出窗口范围后销毁。 |
| 输入控制 | 使用 `sf::Keyboard::isKeyPressed` 轮询键盘状态，实现 WASD 移动。 |
| 窗口限制 | 玩家坦克位置被限制在窗口范围内。 |
| 射击冷却 | 增加基于时间的射击冷却机制。 |
| 子弹数量 | 限制玩家同屏自身炮弹数量。 |

### 测试与验证

| 测试内容 | 结果 |
| -- | -- |
| 近距离和远距离射击 | 炮弹移动与冷却表现符合当时参数设计。 |
| 同屏炮弹数量 | 在当前速度和冷却参数下，炮弹数量限制表现符合预期。 |

## 2026-06-27 面向对象重构与实体扩展

### 阶段目标

将早期逻辑重构为面向对象结构，建立玩家、敌人、基地和道具等实体。

### 涉及内容

| 模块 | 内容 |
| -- | -- |
| `Tank` | 抽取坦克基类，封装生命值、速度、基础位移、受击等共有数据和行为。 |
| `PlayerTank` | 派生玩家坦克，负责玩家输入、射击和弹药强化倒计时。 |
| `EnemyTank` | 派生敌方坦克，区分轻型与重型数值，并预留移动方向与射击接口。 |
| `Base` | 添加基地实体，支持生命值和受击。 |
| `Item` | 添加道具实体，支持血包和弹药强化包两种类型。 |

### 阶段结果

项目形成以 `Tank` 为基础的继承结构，玩家、敌人、基地、道具和炮弹可以在同一游戏循环中协作。

## 2026-06-27 世界逻辑整合与碰撞结算

### 阶段目标

将实体更新、碰撞检测和资源回收整合到 `Game` 主控类。

### 涉及内容

| 类别 | 内容 |
| -- | -- |
| `Game` 主控类 | 接管原本分散在入口文件中的游戏循环和实体管理逻辑。 |
| 容器管理 | 使用 `std::vector` 管理敌人、炮弹和道具。 |
| 碰撞检测 | 使用 SFML 矩形交叠检测处理炮弹命中、道具拾取等事件。 |
| 资源回收 | 遍历容器时使用迭代器和 `erase` 安全移除失效对象。 |

### 修复记录

| 问题 | 修复 |
| -- | -- |
| 弹药强化拾取后表现和伤害状态异常 | 调整强化状态计时器和伤害重置逻辑。 |
| 敌方坦克受击后生命值未正确扣除 | 修正 `Tank::takeDamage` 中伤害计算逻辑。 |

## 2026-06-28 机制联调与移交前整理

### 阶段目标

对基础机制进行编译、联调和移交前整理。

### 涉及内容

| 类别 | 内容 |
| -- | -- |
| 编译检查 | 对核心机制代码进行清理构建和联调检查。 |
| 实机测试 | 检查玩家移动、强化道具、敌人受击和摧毁表现。 |
| 道具验证 | 弹药强化伤害提升到 30，持续 15 秒后恢复普通伤害。 |
| 敌人数值 | 轻型和重型敌人的生命值与受击表现符合当时设定。 |
| 接口整理 | 稳定 `EnemyTank` 等接口，便于后续 AI 和地图碰撞接入。 |

### 阶段结果

接手前已有基础战斗原型，包括玩家、敌人、炮弹、基地、道具、基础碰撞和胜负判断雏形。

## 2026-06-29 地图瓦片与地图加载

### 阶段目标

建立地图瓦片和文本地图加载基础，为后续地形碰撞做准备。

### 涉及文件

| 文件 | 内容 |
| -- | -- |
| `include/world/tile.hpp` | 新增 `TileType` 和 `Tile` 接口。 |
| `src/world/tile.cpp` | 实现瓦片属性、字符转换、颜色和绘制层级。 |
| `include/world/map.hpp` | 新增 `Map` 地图管理类接口。 |
| `src/world/map.cpp` | 实现文本地图加载、瓦片存储和出生点读取。 |
| `assets/maps/map_test.txt` | 新增地图加载测试文件。 |
| `tests/map_test.cpp` | 新增最小地图加载测试代码。 |

### 实现内容

| 模块 | 内容 |
| -- | -- |
| `Tile` | 支持空地、砖墙、钢墙、水域、草地。 |
| 地形规则 | 提供坦克阻挡、炮弹阻挡、可破坏、是否绘制、绘制层级等判断。 |
| 字符映射 | 支持 `.`、`#`、`S`、`W`、`G`、`P`、`E`、`B` 等地图字符。 |
| `Map` | 使用一维 `std::vector<Tile>` 保存地图格子。 |
| 坐标换算 | 使用 `index=y*width_+x` 将二维格子坐标转换为一维下标。 |
| 出生点 | 支持读取玩家出生点、敌人出生点和基地位置。 |

### 测试记录

| 测试项 | 结果 |
| -- | -- |
| `assets/maps/map_test.txt` 加载 | 符合预期。 |
| 地图宽度、高度、出生点、基地位置和瓦片类型检查 | 符合预期。 |
| `tests/map_test.cpp` 预期输出 | `map_test passed`。 |

## 2026-06-29 地图接入游戏主流程

### 阶段目标

将玩家、基地和敌人的初始位置从固定坐标改为优先读取地图文件。

### 涉及文件

| 文件 | 内容 |
| -- | -- |
| `include/core/game.hpp` | 增加 `Map` 成员，玩家和基地改为可选对象。 |
| `src/core/game.cpp` | 加载测试地图，并根据地图点位创建玩家、基地和敌人。 |

### 实现内容

| 内容 | 说明 |
| -- | -- |
| 地图加载 | 游戏启动时加载 `assets/maps/map_test.txt`。 |
| 玩家生成 | 如果地图中存在 `P`，玩家从 `P` 所在格子中心生成。 |
| 基地生成 | 如果地图中存在 `B`，基地从 `B` 所在格子中心生成。 |
| 敌人生成 | 如果地图中存在 `E`，敌人从所有 `E` 点生成，并交替使用轻型和重型类型。 |
| 兜底逻辑 | 地图加载失败或缺少敌人出生点时保留固定坐标生成逻辑。 |
| 渲染顺序 | 地图先绘制，随后绘制基地、道具、玩家、敌人和炮弹。 |

### 验证记录

执行 Debug 构建通过：

```bash
cmake --build --preset debug
```

## 2026-06-29 地形碰撞模块

### 阶段目标

建立地图地形与坦克、炮弹之间的碰撞判断。

### 涉及文件

| 文件 | 内容 |
| -- | -- |
| `include/world/collision.hpp` | 新增碰撞判断接口。 |
| `src/world/collision.cpp` | 实现矩形与地图格子的碰撞检测。 |
| `CMakeLists.txt` | 将 `src/world/collision.cpp` 加入主程序编译。 |
| `include/entity/tank.hpp` | 新增位置和目标位置碰撞矩形辅助接口。 |
| `src/entity/tank.cpp` | 实现位置读写和 `boundsAt`。 |
| `include/entity/player_tank.hpp` | 增加带地图和坦克阻挡列表的更新接口。 |
| `src/entity/player_tank.cpp` | 玩家移动时接入地图和坦克碰撞判断。 |
| `include/entity/enemy_tank.hpp` | 增加带地图和坦克阻挡列表的更新接口。 |
| `src/entity/enemy_tank.cpp` | 敌人移动时接入地图和坦克碰撞判断。 |
| `src/core/game.cpp` | 组装阻挡矩形列表并调用碰撞版更新接口。 |

### 实现内容

| 功能 | 状态 |
| -- | -- |
| 坦克与砖墙、钢墙、水域碰撞 | 已接入。 |
| 炮弹与砖墙、钢墙碰撞 | 已接入。 |
| 炮弹飞出地图边界后删除 | 已接入。 |
| 玩家与敌方坦克碰撞 | 已接入。 |
| 敌人与玩家碰撞 | 已接入。 |
| 敌人与敌人碰撞 | 已接入。 |
| 敌人撞墙或撞坦克后的换向 | 暂未实现，当前保持原方向继续尝试移动。 |

### 关键设计

| 设计点 | 说明 |
| -- | -- |
| `Collision::hitsTankBlock` | 判断坦克矩形是否碰到阻挡坦克的地图格。 |
| `Collision::hitsBulletBlock` | 判断炮弹矩形是否碰到阻挡炮弹的地图格。 |
| `Collision::hitsMapBounds` | 判断矩形是否超出地图范围。 |
| `Tank::boundsAt` | 在实际移动前预测目标位置的碰撞矩形。 |
| 坦克阻挡列表 | `Game` 为玩家和敌人分别构造其他坦克的 `sf::FloatRect` 列表。 |

### 验证记录

执行 Debug 构建通过：

```bash
cmake --build --preset debug
```

## 2026-06-29 砖墙破坏机制

### 阶段目标

在炮弹与地图碰撞基础上实现砖墙被击毁。

### 涉及文件

| 文件 | 内容 |
| -- | -- |
| `include/world/collision.hpp` | 新增炮弹命中地图格子的查询接口。 |
| `src/world/collision.cpp` | 实现 `findBulletHitTile`。 |
| `src/core/game.cpp` | 命中砖墙时将瓦片改为空地。 |

### 实现内容

| 情况 | 当前效果 |
| -- | -- |
| 子弹打到砖墙 | 砖墙消失，子弹消失。 |
| 子弹打到钢墙 | 钢墙保留，子弹消失。 |
| 子弹打到草地、水域、空地 | 子弹继续飞行。 |
| 子弹飞出地图边界 | 子弹消失。 |

### 验证记录

执行 Debug 构建通过：

```bash
cmake --build --preset debug
```

## 2026-06-29 坦克实体碰撞接口整理

### 阶段目标

删除只支持地图碰撞的中间重载接口，保留基类要求接口和当前主循环使用的最终碰撞接口。

### 涉及文件

| 文件 | 内容 |
| -- | -- |
| `include/entity/player_tank.hpp` | 删除地图-only 中间更新和移动接口声明。 |
| `src/entity/player_tank.cpp` | 删除地图-only 中间更新和移动函数定义。 |
| `include/entity/enemy_tank.hpp` | 删除地图-only 中间更新接口声明。 |
| `src/entity/enemy_tank.cpp` | 删除地图-only 中间更新函数定义。 |
| `src/core/game.cpp` | 保持调用最终版地图加坦克阻挡列表接口。 |

### 保留接口

| 类 | 接口 | 用途 |
| -- | -- | -- |
| `PlayerTank` | `update(float deltaTime,std::vector<Bullet>& bullets) override` | 满足 `Tank` 基类纯虚函数要求。 |
| `PlayerTank` | `update(...,const Map& map,const std::vector<sf::FloatRect>& tankBlocks)` | 当前游戏主流程使用。 |
| `EnemyTank` | `update(float deltaTime,std::vector<Bullet>& bullets) override` | 满足 `Tank` 基类纯虚函数要求。 |
| `EnemyTank` | `update(...,const Map& map,const std::vector<sf::FloatRect>& tankBlocks)` | 当前游戏主流程使用。 |

### 验证记录

执行 Debug 构建通过：

```bash
cmake --build --preset debug
```

## 2026-06-29 草地覆盖与地图分层绘制

### 阶段目标

实现草地绘制在坦克、基地、道具和炮弹之上，形成经典坦克大战中的遮挡效果。

### 涉及文件

| 文件 | 内容 |
| -- | -- |
| `include/world/map.hpp` | 新增 `drawLayer(sf::RenderWindow& window,int layer) const`。 |
| `src/world/map.cpp` | 实现分层绘制。 |
| `src/world/tile.cpp` | 修改 `Tile::drawLayer` 的层级约定。 |
| `src/core/game.cpp` | 调整渲染顺序。 |

### 层级约定

| 层级 | 内容 |
| -- | -- |
| 0 | 地图底层，包括砖墙、钢墙、水域等非草地地形。 |
| 1 | 地图上层，目前用于草地。 |

### 当前绘制顺序

```text
地图底层
基地、道具、玩家、敌人、炮弹
地图上层
```

### 当前效果

| 项目 | 状态 |
| -- | -- |
| 草地覆盖坦克和炮弹 | 已实现。 |
| 草地阻挡坦克 | 不阻挡。 |
| 草地阻挡炮弹 | 不阻挡。 |
| 后续 HUD、暂停菜单、设置界面扩展 | 绘制顺序已预留空间，具体 UI 尚未实现。 |

### 验证记录

执行 Debug 构建通过：

```bash
cmake --build --preset debug
```

## 2026-06-29 UI 模块规划

### 阶段目标

确定后续 HUD 和菜单界面的模块归属，避免继续把显示逻辑堆入 `Game`、`Map` 或实体类。

### 设计决策

| 项目 | 决策 |
| -- | -- |
| UI 模块位置 | 计划新增 `include/ui/` 和 `src/ui/`。 |
| 首个 UI 类 | 计划新增 `Hud`。 |
| 首批显示内容 | 玩家血量、剩余敌人数、弹药强化状态。 |
| 绘制顺序 | 地图底层、实体、地图上层之后，再绘制 HUD。 |
| 后续扩展 | 暂停菜单、设置界面和胜负提示继续放在 UI 层。 |

### 计划文件

- `include/ui/hud.hpp`
- `src/ui/hud.cpp`

### 当前状态

仅完成文档规划，尚未创建 UI 代码文件。

## 2026-06-29 基地阻挡坦克移动

### 阶段目标

修复基地只作为受击目标、不阻挡坦克移动的问题，使玩家坦克和敌方坦克都不能穿过基地。

### 涉及文件

- `src/core/game.cpp`

### 实现内容

| 文件 | 行号 | 内容 | 说明 |
| -- | -- | -- | -- |
| `src/core/game.cpp` | 77-78 | 将 `base_->getBounds()` 加入 `playerTankBlocks` | 玩家移动时会把基地视为阻挡矩形。 |
| `src/core/game.cpp` | 90-91 | 将 `base_->getBounds()` 加入 `enemyTankBlocks` | 敌人移动时会把基地视为阻挡矩形。 |

### 当前效果

- 玩家坦克不能穿过基地。
- 敌方坦克不能穿过基地。
- 敌方炮弹仍可命中基地并造成伤害。
- 玩家炮弹当前不会伤害基地。

### 构建验证

已执行 Debug 构建并通过：

```bash
cmake --build --preset debug
```

## 2026-07-01 AI 与网络通信阶段记录

### 7.1 敌方 AI 头文件编写

#### 阶段目标

建立敌方坦克 AI 的头文件结构，为后续手写简单 AI、普通 AI 和困难 AI 做准备。

#### 涉及文件

| 文件 | 内容 |
| -- | -- |
| `include/ai/tank_command.hpp` | 定义 AI 输出给坦克执行的命令结构。 |
| `include/ai/ai_context.hpp` | 定义 AI 决策所需的只读上下文。 |
| `include/ai/ai_controller.hpp` | 定义 AI 控制器抽象基类。 |
| `include/ai/enemy_ai.hpp` | 编写 `EasyAI`、`NormalAI`、`HardAI` 的类声明。 |
| `CMakeLists.txt` | 将 AI 头文件加入项目目标，便于构建系统和 IDE 跟踪。 |

#### 当前内容

| 模块 | 内容 |
| -- | -- |
| `TankCommand` | 包含方向、是否移动、是否开火。 |
| `AIContext` | 预留地图、玩家、基地、敌人和炮弹等只读输入。 |
| `AIController` | 提供 `decide` 纯虚函数。 |
| `EasyAI` | 预留随机方向、受阻判断、决策计时和开火概率相关接口。 |
| `NormalAI` | 预留巡逻、追击、攻击状态和路径相关接口。 |
| `HardAI` | 预留目标评分、攻击基地、攻击玩家和破墙相关接口。 |

#### 当前状态

已完成头文件编写和构建列表接入，尚未完成 `.cpp` 实现，尚未接入游戏循环，尚未完成运行测试。

### 7.2 网络通信模块编写

#### 阶段目标

整理网络通信组员提供的临时代码，将网络消息、会话管理和游戏主循环接入到当前工程结构中。

#### 涉及文件

| 文件 | 内容 |
| -- | -- |
| `include/network/network_types.hpp` | 定义网络模式、连接状态、玩家输入和游戏快照数据结构。 |
| `include/network/network_message.hpp` | 定义网络消息打包和读取接口。 |
| `include/network/network_session.hpp` | 定义主机、客户端连接和消息收发接口。 |
| `src/network/network_message.cpp` | 实现输入、快照、地块变化等消息序列化和反序列化。 |
| `src/network/network_session.cpp` | 实现主机监听、客户端连接、消息轮询和发送。 |
| `include/core/game.hpp` | 增加网络会话、双玩家、输入状态和快照相关接口。 |
| `src/core/game.cpp` | 增加离线、主机和客户端三种更新流程。 |
| `include/entity/tank.hpp` / `src/entity/tank.cpp` | 增加方向和生命值同步所需接口。 |
| `include/entity/player_tank.hpp` / `src/entity/player_tank.cpp` | 增加由外部输入驱动玩家更新的接口。 |
| `include/entity/base.hpp` / `src/entity/base.cpp` | 增加基地生命值读取和设置接口。 |
| `include/entity/bullet.hpp` | 增加炮弹位置和速度读取接口。 |
| `CMakeLists.txt` | 将网络源文件和头文件加入项目目标。 |

#### 当前内容

| 模块 | 内容 |
| -- | -- |
| 网络模式 | 预留离线、主机和客户端模式。 |
| 输入同步 | 客户端向主机发送玩家输入。 |
| 主机权威 | 主机更新游戏世界并向客户端发送游戏快照。 |
| 快照同步 | 快照中包含玩家、敌人、炮弹、道具和基地状态。 |
| 地图同步 | 砖墙被破坏时可发送地块变化消息。 |
| 双玩家 | 主机连接客户端后创建第二名玩家并参与碰撞和受击。 |

#### 当前状态

已完成网络通信代码整理、目录归档、正式 `Game` 接入和 Debug 构建检查；尚未完成双端实机连接测试，尚未完成局域网同步稳定性验证，尚未完成 UI 入口接入。
