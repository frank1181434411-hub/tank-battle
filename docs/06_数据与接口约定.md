# 数据与接口约定

## 1. 基础枚举

当前存在于 `include/entity/entity.hpp`：

```cpp
enum class Direction { Up, Down, Left, Right };
enum class Faction { Player, Enemy };
```


当前存在于 `include/entity/enemy_type.hpp`：

```cpp
enum class EnemyType { Light,Heavy };
```

当前存在于 `include/entity/item.hpp`：

```cpp
enum class ItemType { HealthPack, DamagePack };
```

当前存在于 `include/core/game.hpp`：

```cpp
enum class GameStatus { Running, Victory, Defeat };
```

## 2. 坦克数据

`Tank` 当前保存位置、方向、速度、生命值、阵营、当前炮弹伤害、射击冷却、炮弹速度、实体 ID 和 SFML 矩形形状。

公开接口包括：

- `update`
- `draw`
- `takeDamage`
- `heal`
- `isAlive`
- `getHp`
- `getMaxHp`
- `getId`
- `getBounds`
- `getPosition`
- `setPosition`
- `getDirection`
- `setDirection`
- `setHp`
- `boundsAt`

## 3. 炮弹数据

`Bullet` 当前保存：

- `position_`
- `velocity_`
- `damage_`
- `ownerId_`
- `faction_`
- `isAlive_`
- `shape_`

公开接口包括 `update`、`draw`、`isAlive`、`destroy`、`getDamage`、`getOwnerId`、`getFaction`、`getBounds`、`getPosition`、`getVelocity`。

## 4. 道具数据

`Item` 当前保存道具类型、存活状态和矩形形状。已有类型为血包和弹药强化包。

公开接口包括 `draw`、`isAlive`、`pickUp`、`getType`、`getBounds`。

## 5. 基地数据

`Base` 当前保存生命值和矩形形状。当前 `Game` 会把 `Base::getBounds()` 加入玩家和敌人的移动阻挡列表。

公开接口包括 `takeDamage`、`isAlive`、`getHp`、`setHp`、`draw`、`getBounds`。

## 6. 地图数据

当前地图系统包含 `TileType`、`Tile` 和 `Map`。

`TileType` 包含：

```cpp
enum class TileType
{
    Empty,
    Brick,
    Steel,
    Water,
    Grass
};
```

`Tile` 提供：

- `type`
- `setType`
- `blocksTank`
- `blocksBullet`
- `isDestructible`
- `isDrawable`
- `drawLayer`
- `toChar`
- `fromChar`
- `color`

### 6.1 敌人初始部署数据

增加敌人初始部署结构：

```cpp
struct EnemySpawn
{
    sf::Vector2f position;
    EnemyType type=EnemyType::Light;
};
```

`EnemySpawn` 只保存敌人的初始位置和物理类型，不保存 AI 难度。

`Map` 保存：

- 地图宽度和高度；
- 瓦片数组；
- 玩家初始位置；
- 基地位置；
- `std::vector<EnemySpawn>` 敌人初始部署列表；
- 地图加载错误信息。

`Map` 对外提供：

```cpp
const sf::Vector2f& playerSpawn() const noexcept;
const sf::Vector2f& basePosition() const noexcept;
const std::vector<EnemySpawn>& enemySpawns() const noexcept;
```

### 6.2 地图字符约定

| 字符 | 含义 | 数据处理 |
| --- | --- | --- |
| `.` | 空地 | 保存为空地 |
| 空格 | 空地 | 保存为空地 |
| `#` | 砖墙 | 保存为砖墙 |
| `S` | 钢墙 | 保存为钢墙 |
| `W` | 水域 | 保存为水域 |
| `G` | 草地 | 保存为草地 |
| `P` | 玩家初始位置 | 记录位置，格子保存为空地 |
| `B` | 基地位置 | 记录位置，格子保存为空地 |
| `L` | 轻型敌人初始位置 | 添加 `EnemyType::Light`，格子保存为空地 |
| `H` | 重型敌人初始位置 | 添加 `EnemyType::Heavy`，格子保存为空地 |

字符 `E` 不再是合法的敌人初始位置标记。

### 6.3 敌人数量约定

关卡敌人数量直接由地图决定：

```text
轻型敌人数量 = L 的数量
重型敌人数量 = H 的数量
敌人总数 = L 的数量 + H 的数量
```

地图加载完成后，`Game` 一次性创建全部敌人。敌人死亡后不会重新添加到容器，也不会生成新的替补敌人。

当全部初始敌人被消灭，敌人容器为空时，游戏进入胜利状态。

### 6.4 AI 难度约定

`EnemySpawn::type` 只控制轻型和重型坦克的生命值、移动速度、伤害和射击冷却等物理属性。

`EasyAI`、`NormalAI` 和 `HardAI` 的选择由游戏难度或关卡配置决定，不写入地图字符，避免将坦克物理类型与 AI 策略强制绑定。

例如，同一个 `H` 标记生成的重型坦克，可以根据当前游戏难度使用 `EasyAI`、`NormalAI` 或 `HardAI`。

### 6.5 地图合法性约定

地图加载时应检查：

- 地图宽度和高度合法；
- 玩家初始位置存在；
- 基地位置存在；
- 至少存在一个 `L` 或 `H`；
- 所有实体标记均位于地图范围内；
- 不存在无法识别的字符；
- 不再接受旧的 `E` 标记；
- 同一个地图格不能同时表示多个实体；
- 敌人、玩家和基地初始位置对应的基础瓦片应按空地处理。

如果地图格式不合法，`Map` 应返回加载失败，并保存明确的错误信息。

## 7. 碰撞接口

当前存在于 `include/world/collision.hpp`：

- `hitsTankBlock`
- `hitsBulletBlock`
- `hitsMapBounds`
- `findBulletHitTile`

这些接口使用 `sf::FloatRect` 与地图瓦片进行碰撞判断。当前用于玩家和敌人的地形阻挡、炮弹撞墙、砖墙破坏和炮弹越界删除。坦克之间和基地对坦克的阻挡使用 `sf::FloatRect::findIntersection` 判断。

## 8. 配置数据

状态：待设计。`config/game_config.ini` 存在但为空，当前代码未读取配置文件。

## 9. AI 输入和输出

状态：部分完成。

当前存在：

- `TankCommand`：包含 `Direction direction`、`bool move`、`bool fire`。
- `AIContext`：包含地图、玩家列表、基地、敌人列表和炮弹列表的只读引用。
- `AIController::decide`：输入 `EnemyTank`、`AIContext` 和 `deltaTime`，输出 `TankCommand`。
- `EasyAI`：已实现随机方向移动、受阻检测和概率开火。
- `NormalAI`：已实现目标选择、直线射击判断、BFS 路径、射击位收集、权重 2 射击位追击和卡住后重新规划。
- `HardAI`：已实现目标评分、基地/玩家目标选择、权重 1 射击位寻路、砖墙攻击和受阻处理的初步策略。

当前 `Game` 为敌人维护 `std::vector<std::unique_ptr<AIController>>`，每个敌人拥有独立 AI 控制器。AI 输出 `TankCommand` 后，由 `EnemyTank::setMoveDirection`、`EnemyTank::stopMoving`、`EnemyTank::setDirection` 和 `EnemyTank::tryFire` 执行动作。

普通 AI 的射击位权重约定：

| 权重 | 含义 | 当前使用 |
| -- | -- | -- |
| 2 | 炮弹直线可达，路径上没有砖墙和钢墙；水域可跨射但不能作为站立点 | `NormalAI` 当前只选择权重 2 |
| 1 | 需要穿过砖墙后才可射击 | `HardAI` 可在权重 2 路径代价过大时选择 |
| 0 | 被钢墙等不可摧毁地形阻断 | 不选择 |

## 10. 网络消息

状态：已建立接口，未完成双端实机测试。

当前存在于 `include/network/`：

- `NetworkMode`：`Offline`、`Host`、`Client`。
- `NetworkState`：`Disconnected`、`Connecting`、`Connected`、`Hosting`、`Failed`。
- `PlayerInputState`：记录移动和射击输入。
- `TankSnapshot`：记录坦克 ID、位置、方向、生命值和存活状态。
- `BulletSnapshot`：记录炮弹位置、阵营、伤害、拥有者、存活状态和速度方向。
- `ItemSnapshot`：记录道具位置、类型和存活状态。
- `GameSnapshot`：记录游戏状态、基地状态、玩家、敌人、炮弹、道具和已破坏砖墙列表。

当前 `NetworkSession` 支持主机监听、客户端连接、断开、更新、输入发送、快照发送、地块变化发送，以及对应消息轮询。实际局域网连接和同步稳定性尚未测试。

## 11. UI 显示数据

状态：计划设计。后续 `ui/` 模块计划读取以下只读数据：

| 数据 | 来源 | 用途 |
| -- | -- | -- |
| 玩家当前生命值 | `PlayerTank::getHp` | HUD 血量显示 |
| 玩家最大生命值 | `PlayerTank::getMaxHp` | HUD 血量比例 |
| 剩余敌人数 | `Game` 中存活敌人数量 | HUD 敌人剩余显示 |
| 弹药强化状态 | `PlayerTank::hasDamageBuff` | HUD 强化状态提示 |
| 弹药强化剩余时间 | 待新增接口 | HUD 强化倒计时 |

计划新增 `Hud` 类，具体接口待实现时确定。

## 12. 地图文件格式

当前采用简单文本地图格式，已在 `assets/maps/single/map_test.txt` 中使用。

地图目录当前约定：

| 目录 | 用途 |
| -- | -- |
| `assets/maps/single/` | 单人模式和单人手动测试地图 |
| `assets/maps/coop/` | 预留双人联机地图，目前仅保留目录 |

| 字符 | 含义 |
| -- | -- |
| `.` | 空地 |
| 空格 | 空地 |
| `#` | 砖墙 |
| `S` | 钢墙 |
| `W` | 水域 |
| `G` | 草地 |
| `P` | 玩家出生点，地形按空地处理 |
| `L` | 轻型敌方坦克初始位置，地形按空地处理 |
| `H` | 重型敌方坦克初始位置，地形按空地处理 |
| `B` | 基地位置，地形按空地处理 |

## 13. 保存文件格式

状态：待设计。

## 14. 接口变更记录

| 日期 | 变更内容 | 说明 |
| -- | -- | -- |
| 2026-07-01 | 新增 AI 接口头文件 | 建立 `TankCommand`、`AIContext`、`AIController` 和 `enemy_ai.hpp` 类声明 |
| 2026-07-01 | 新增网络通信接口 | 建立网络类型、消息、会话和快照同步相关接口 |
| 2026-07-02 | 新增 AI 行为实现 | 实现 `EasyAI`、`NormalAI` 初步决策，并新增 `TankBattleAITest` 测试目标 |
| 2026-07-02 | 地图目录拆分 | 将已有地图整理到 `assets/maps/single/`，预留 `assets/maps/coop/` |
| 2026-07-02 | 新增 HardAI 初步实现 | 实现困难 AI 目标评分、权重 1 射击位和砖墙攻击初步策略 |
